[zentao-api](../index.md) / pickFieldsSingle

# Function: pickFieldsSingle()

> **pickFieldsSingle**(`data`, `fields`): [`DataRecord`](../type-aliases/DataRecord.md)

从单条对象中摘取指定字段，支持通过 `.` 访问子字段，保留嵌套结构。

## Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | [`DataRecord`](../type-aliases/DataRecord.md) |
| `fields` | `string`[] |

## Returns

[`DataRecord`](../type-aliases/DataRecord.md)
